 <% @users.each do |user| %>
    

      <% if user.usename != current_user.usename %>
        <%=h user.usename %>
        <%= link_to "Add Friend", users_path(:friend_id => user) %>
      <% end %>
    
    
  <% end %>